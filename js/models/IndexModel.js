var IndexModel = Backbone.Model.extend({
    defaults: {
        templateValues: {
            title: 'index',
            desctiption: 'this is index page'
        }
    },

    initialize: function() {
    }
});

