var BaseModel = Backbone.Model.extend({
    defaults: {
        templateValues: {
            title: 'index page',
            desctiption: 'this is index page'
        }
    },
    initialize: function() {
    }
});

